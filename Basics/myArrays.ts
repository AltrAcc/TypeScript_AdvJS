const heros: string[] = [];
// const heroPower: number[] = [];
const heroPower: Array<number> = [];

type User = {
   name: string
   isActive: boolean
}

const allUsers: User[] = [];

const MLModels: number[][] = [
   [2555, 255, 255], [], []
]

heros.push('salu');
heros.push('kalu');

heroPower.push(3);

allUsers.push({name: 'salu', isActive: true});

